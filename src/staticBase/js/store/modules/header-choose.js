define(function(require, exports, module) {
  // initial state
  const state = {
    isShow : false
  }

  // getters
  const getters = {
    
  }

  // actions
  const actions = {
 
  }

  // mutations
  const mutations = {
   
  }

  return  {
    state,
    getters,
    actions,
    mutations
  }
})
