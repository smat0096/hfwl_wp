define(function(require, exports, module) {
  
  // initial state
  var state = {

  }
  // getters
  var getters = {

  }

  // actions
  var actions = {
    
  }

  // mutations
  var mutations = {
    user_init : function(state,user){
    }
  };

  return  {
    state : state,
    getters : getters,
    actions : actions,
    mutations : mutations
  }
})
